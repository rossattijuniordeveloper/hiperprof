import 'package:flutter/material.dart';

class PesquisaProfessorView extends StatelessWidget {
  const PesquisaProfessorView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
